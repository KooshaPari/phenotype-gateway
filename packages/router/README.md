# packages/router

Promotion target for Rust/Zig router revamp (`spikes/rust/router`).

Criteria: [docs/PROMOTION.md](../../docs/PROMOTION.md). OmniRoute interim — not long-term canonical.

**H10:** `ComboVariant` routing table (6 auto-combo variants) + delegate targets; re-exports spike lib.
